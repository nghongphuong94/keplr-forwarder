{
  "name": "keplr-forwarder",
  "version": "1.0.0",
  "description": "Forwarding page for one-click onboarding",
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "license": "MIT",
  "files": [
    "/dist"
  ],
  "scripts": {
    "start": "static-server ./dist --port 9010",
    "deploy": "./deploy.sh",
    "deploy-to-firebase": "firebase deploy --only hosting:keplr-forwarder"
  },
  "devDependencies": {
    "static-server": "^2.2.1"
  }
}
